import { createTheme } from "@mui/material";

export const appTheme = createTheme({

    palette: {
        mode: 'dark',
        primary: { main: "#F5f5f1" },
        secondary: { main: "#E50914" },
        text: { primary: "#F5f5f1" }
    }
});